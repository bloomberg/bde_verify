#!/usr/bin/make -f
# -*- makefile -*-
include $(DISTRIBUTION_REFROOT)/opt/bb/share/bbtoolchain-cmake-debhelper/bbtoolchain-cmake-debhelper-macros.mk

LLVMDIR ?= $(DISTRIBUTION_REFROOT)$(LIBDIR_32BIT)/llvm-11.0
DEBHELPER_CMAKE_ARGS = -DClang_DIR=$(LLVMDIR)/lib64/cmake/clang/
#LDFLAGS:=-L$(DISTRIBUTION_REFROOT)$(LIBDIR_64BIT) $(BLOOMBERG_RPATH_64BIT)

$(eval $(call BB_DEBHELPER_BIARCH_BUILD,64))
$(eval $(call BB_DEBHELPER_PACKAGE,bde-verify, bde-verify))
$(eval $(call BB_DEBHELPER_PACKAGE,libbde-verify-dev, bde-verify-dev))

include $(DISTRIBUTION_REFROOT)/opt/bb/share/bbtoolchain-cmake-debhelper/bbtoolchain-cmake-debhelper-rules.mk
